# 미션 자동 판정 설계

> **원칙**: 모든 미션은 앱이 몇 초 안에 자동 판정한다. 교사는 판정 루프에 들어가지 않는다.
> **두 개의 문턱**: 게임 보상은 낮은 문턱, 수행평가 통과는 높은 문턱.
> **보장**: 성실하게 하면 반드시 통과한다. 통과 조건을 제출 전에 학생에게 미리 보여준다.

---

## 1. 판정 결과는 세 가지

| 판정 | 조건 | 게임 보상 | 수행평가 |
|---|---|---|---|
| **통과** | 구조화 항목 기준 충족 + 서술 성실 조건 모두 충족 | 100% | 통과 |
| **소략 통과** | 구조화 항목은 충족했으나 서술이 기준 미달 | 100% | **소략** 표시 |
| **미통과** | 성실성 조건 미달 | 20% | 미통과 |

게임 보상은 "통과"와 "소략 통과"가 같다. 성적만 갈린다. 이렇게 해야 성적으로 못 이기는 학생도 게임에서는 대등하게 간다.

**평가계획서 요소1 배점과 자동 연결**

| 앱 판정 결과 | 계획서 배점 |
|---|---|
| 3개 모두 통과 | 10점 |
| 3개 통과했으나 하나 이상 **소략** | 8점 |
| 2개 통과 | 6점 |
| 1개 이하 통과 | 4점 |

앱이 점수를 자동 산출한다. 교사는 나머지 10점(개념 4 / 자료해석·표현 3 / 태도 3)만 답안 질로 채점한다.

---

## 2. 성실성 판정 지표

서술 답안에 대해 아래를 **모두** 자동 검사한다.

| # | 지표 | 판정 방법 | 걸리는 것 |
|---|---|---|---|
| 1 | 최소 분량 | 미션별 지정 글자 수 (보통 150~250자) | 한 줄 쓰고 제출 |
| 2 | 핵심어 포함 | 미션별 지정 핵심어 3~5개 중 **2개 이상** | 딴소리 |
| 3 | **자기 데이터 인용** | 학생이 입력한 측정값·배정 수치 중 **2개 이상**이 서술에 등장 | AI 답안, 남의 답안 복붙 |
| 4 | 반복·도배 | 동일 문장 반복, 자모 나열, 무의미 문자열 검출 | "ㅁㄴㅇㄹ", 같은 말 도배 |
| 5 | 중복 제출 | 본인 이전 답안·타 학생 답안과 유사도 검사 | 3교시 학생이 4교시 학생에게 넘긴 답 |
| 6 | 작성 시간 | 입력 시작~제출 시간, 붙여넣기 여부 기록 | 10초 만에 300자 |

**3번이 핵심이다.** 학생마다 배정 수치가 다르고 측정값도 다르기 때문에, 자기 데이터를 인용하지 않고는 통과가 불가능하다. AI 차단 장치가 곧 성실성 판정 장치가 된다.

---

## 3. 미션 유형별 판정 규칙

### ① 난수 자료 해석형

- **구조화 항목**: 분류·순위·계산 → 정답 대조 자동 채점
- **통과 기준**: 구조화 항목 **70% 이상** 정답 + 서술 성실 조건 충족
- **자기 데이터 인용**: 앱이 배정한 고유 수치(이리듐 농도, 알베도 값 등)가 서술에 등장해야 함
- 70% 미만이면 **재도전**. 문항은 같은 유형의 다른 난수로 새로 생성된다

### ② 실측 + 예측 잠금형

- **1단계 예측**: 제출과 동시에 잠금. 맞고 틀림은 채점하지 않는다 (예측은 틀려도 된다)
- **2단계 측정값 입력**: 값의 개수·범위 검증. 물리적으로 불가능한 값은 반려
- **3단계 서술**: 예측과 결과의 차이를 반드시 언급해야 통과
- **통과 기준**: 측정값 입력 완료 + 자기 측정값 2개 이상 인용 + 예측 대비 서술 포함
- 측정값이 없으면 서술 칸 자체가 열리지 않는다

### ③ 현장 데이터 설계·제안형

- **구조화 항목**: 조사한 항목을 표로 입력 (종 이름, 사용량, 좌표 등) — 최소 개수 검증
- **계산 항목**: 탄소발자국·설비 규모 등은 자동 계산 후 학생 답과 대조
- **통과 기준**: 조사 표 최소 개수 충족 + 계산 정확 + 자기 조사 데이터 2개 이상 인용 + 실행 방안에 수치(기간·인원·비용 중 하나) 포함
- "노력하겠습니다" 같은 추상적 제안은 수치 미포함으로 걸러진다

---

## 4. 학생용 성실도 체크리스트 (제출 전 표시)

판정 기준을 숨기지 않는다. 제출 버튼 위에 실시간 체크리스트를 띄운다.

```
[산과 염기 혼합 온도 측정]  현재 상태

  ☑ 예측 제출 완료
  ☑ 측정값 6개 입력  (6/6)
  ☑ 200자 이상 작성  (241자)
  ☐ 내 측정값 2개 이상 인용  (0/2)   ← 아직입니다
  ☑ 핵심어 2개 이상 포함  (중화, 최고점)

  → 조건을 모두 채우면 '통과'로 기록됩니다
```

이 화면이 있으면 "왜 나만 미통과냐"는 항의가 나오지 않는다. 성실하게 쓰면 반드시 통과한다는 것도 학생이 눈으로 확인한다.

---

## 5. 재도전

- **미션당 기본 재도전 2회** — 전원 지급
- 뽑기로 얻는 「재도전 코어」는 그 이상의 여유
- 재도전 시 난수 자료는 새로 생성되고, 실측형은 측정값을 다시 입력한다
- **소략 통과**도 재도전으로 '통과'로 끌어올릴 수 있다

→ 뽑기 운이 나빠도 기본 재도전만으로 만점에 도달할 수 있다.

---

## 6. 어뷰징 대비

| 수법 | 대비 |
|---|---|
| 개발자도구로 크레딧 조작 | **모든 점수·크레딧은 서버에서만 계산.** 브라우저는 화면만 그린다 |
| 새로고침으로 정답 재시도 | 제출 시점에 서버가 기록. 재도전은 횟수 차감 후 새 난수 발급 |
| 반 사이 답안 유출 | 학생별 난수 자료 + 자기 데이터 인용 필수 → 남의 답이 통과 불가 |
| 문제 순서 파악 | 반별·학생별 문항 순서 셔플 |
| 계정 여러 개 | 학번 기반 계정만 존재. 교사가 명단으로 사전 생성 |
| 짜고 배틀 져주기 | 배틀 상대 랜덤 매칭 + 익명 |
| 붙여넣기 | 붙여넣기 차단 + 입력 시간 로그 기록 |

---

## 7. 교사 예외 처리 (일상 운영 아님)

자동 판정이 원칙이지만, 성적 확정 전에 교사가 볼 수 있는 화면 하나는 필요하다.

- **이상 신호 목록**: 작성 시간 이상, 유사도 높음, 재도전 과다인 답안만 모아서 표시
- 여기서만 교사가 통과 여부를 조정할 수 있다
- 정상 답안은 교사가 볼 필요가 없다

성적에 들어가는 판정이므로 최종 책임은 교사에게 있어야 한다. 다만 **일상 운영에서는 교사가 개입하지 않는다.**
