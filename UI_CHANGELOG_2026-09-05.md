# UI 보정 인계 — 2026-09-05

## 작업 목적
기존 게임 분위기(베이지·민트·골드, 푸른숲체, 연구소 배경, 원형 조수)를 유지하면서 CSS만 보정했다. 새 화면이나 새로운 게임 기능을 구현한 작업이 아니다.

## 적용 파일
- 앱 폴더: `C:\Users\user\Desktop\연구소 프로그램`
- 수정: `shinjang_science.html`의 첫 번째 `<style>` 끝에 `UI polish 2026-09-05` 주석부터 시작하는 보정 CSS 추가.
- 이 문서: `UI_CHANGELOG_2026-09-05.md`
- 백업: `shinjang_science.pre-ui-polish-20260905.html.bak` (작업 직전 원본, 버전 관리나 외부 전달에 포함하지 말 것).

## 수정 내용
1. 보조 글자 색 `--dim`, `--dim2`를 각각 `#75695e`, `#887b6b`로 진하게 조정.
2. 로비 요약 카드와 중앙 연구점수의 크기·여백을 축소. 중앙 점수는 PC 38px, 작은 화면 34px. 점수 표시 위치와 개수는 유지.
3. 메뉴 최소 높이 46px, 테두리 hover 효과, 기존 눌림 효과 유지.
4. 설명·안내·카드 통계 글자를 주로 13px로 조정하고 줄 간격 개선. 인라인으로 지정된 모든 글자를 일괄 덮어쓰지는 않음.
5. 조수 초상은 기존 34px에서 44px로 확대. 이름 줄에 wrap 허용, 이름 강조, 카드 패딩·간격 조정.
6. 860px 이상에서 대기/배치 조수 목록 폭을 3:2로 변경. 그 아래는 원래 단일 열 흐름 유지.
7. 테마 목록은 860px 이상 3열, 1100px 이상 4열. 작은 화면은 기존 2열 유지.
8. 뒤로/닫기 버튼 최소 40px, 수거·구매 버튼 최소 38px. 키보드 focus-visible 테두리 추가.
9. 전설 초상·카드의 반복 광채만 약하게 조정. 기존 성공/실패 연출과 포인트 flyGain 시간은 유지.
10. prefers-reduced-motion에서 주요 CSS 애니메이션·포인트 이동 transition 비활성화. JS 기반 조수 이동 루프는 수정하지 않아 그대로 남는다.

## 구현상 주의점
- 템플릿 내부 인라인 스타일과 충돌하는 두 영역만 의도적으로 `!important` 사용: 초상 크기, 테마 grid 열 수. reduced-motion 규칙도 예외적으로 사용.
- 테마 grid는 `.scont>div:has(>.box>img[src^="assets/lab/concepts/"])`로 식별한다. 같은 구조/경로를 변경하면 선택자도 검토해야 한다. `:has` 미지원 브라우저에서는 원래 2열로 남는다.
- 조수 이미지 크기 규칙은 `.assistcard .ahead>img`에만 적용한다. 배경 SVG 속 조수 위치나 실제 이동 좌표는 변경하지 않았다.
- 스타일 끝에 추가한 override 방식이다. 이후 AI는 기존 선언을 무작정 중복 추가하지 말고 이 블록부터 확인할 것.

## 변경하지 않은 범위
모든 script 내용, HTML 요소·클래스·ID·이벤트 속성, 인증, 서버 통신, 점수, 재화, 뽑기, 강화, 과제 제출, SQL, supabase-config.js, roundengine.js, roundstyle.css, 캐릭터/배경 assets를 변경하지 않았다. 배포·커밋·푸시는 하지 않았다.

## 검증
- 적용 전 원본과 준비한 수정본의 모든 script 블록 내용 동일함을 비교 확인.
- 추가 CSS 블록을 제거하면 기존 HTML 전체와 동일함을 비교 확인.
- 적용 시 원본이 준비 이후 바뀌지 않았는지 확인하고 백업 후 복사.
- 원본과 백업 및 적용 파일 해시 확인은 실행 결과로 검증.
- 이번 변경 후 브라우저 실화면·모바일 레이아웃·로그인 이후 버튼 회귀 검증은 미실시. 이전 검토에서 원래 앱의 화면 이동을 확인했지만 이번 변경의 회귀 테스트로 간주하면 안 된다.

## 다음 확인 권장
PC 1280×720, 1024px 및 모바일 390px/360px에서 로비, 조수 연구실(긴 이름·배치 조수), 연구 활동, 테마 목록의 줄바꿈과 스크롤을 확인한다. 특히 넓은 화면의 우측 배치 목록이 좁아지므로 버튼 줄바꿈을 확인한다. 포인트·제출 테스트는 별도로 승인된 테스트 환경에서 수행한다.

## 되돌리기
이 작업 뒤 다른 수정이 없다면 백업을 `shinjang_science.html`로 복원한다. 후속 수정이 있으면 파일 전체 복원 대신 첫 style 끝에 추가된 `UI polish 2026-09-05` 블록만 제거한다.

## 기존에 발견했지만 이번에 수정하지 않은 점
로비의 '성적과 무관' 안내와 랭킹의 '1~10등 수행평가 +1점' 안내가 혼동될 수 있다. 평가 정책 확인이 필요한 내용이라 이번 CSS 보정에서는 문구를 바꾸지 않았다.

이 문서에는 계정 비밀번호, 학생 데이터, 서버 설정, script 원문, SQL을 포함하지 않았다.

## 후속 검증 (Claude, 같은 날 이어서 진행)

### 검증 방법
로컬 서버로 PC 1280×720, 1024×720과 모바일 375×812, 360×740에서 실제 로그인 후 로비, 조수 연구실(대기 2~3명 + 배치 1명 조합), 연구 활동, 연구실 테마 화면을 열어 레이아웃과 버튼 동작을 확인했다. 콘솔 에러도 함께 확인했다.

### 결과: CSS 보정 자체는 깨짐 없음
- 로비: 요약 카드·중앙 점수·메뉴 버튼 모두 정상. 860px 이상에서 사이드바(우측 메뉴) 레이아웃도 정상.
- 조수 연구실: `.assistSplit`이 3:2로 바뀐 것 확인, 대기/배치 칸 모두 카드 안 버튼(강화·해고·로비로 돌아오게 하기)이 옆으로 삐져나오지 않고 줄바꿈됨. 1024px, 모바일 모두 확인.
- 연구실 테마: 1280px 4열, 1024px 3열, 모바일(375/360) 2열 — 문서에 적힌 그대로 동작.
- 연구 활동: 진행 중 논문 카드·즉시 작성 버튼 정상.
- 인계 문서에서 우려했던 "우측 배치 목록이 좁아지며 버튼 줄바꿈" 지점은 실제로는 문제 없었다(이전에 `.scont` 최대 너비를 넓혀둔 값과 맞물려 여유 있음).

### CSS와 무관하게 발견해서 별도로 고친 것 (SQL, 이번 UI 작업 범위 밖)
검증 도중 조수를 연구동에 배치 후 수거하는 서버 함수(`lab_collect_dept_production`)에서 PL/pgSQL 변수명이 테이블 컬럼명(`theme`)과 겹쳐 "column reference ambiguous" 오류가 실제로 발생하고 있는 것을 확인했다. UI와 무관한 라이브 버그라 `supabase_schema.sql`에서 변수명만 `a_theme`로 바꿔 수정하고 실제 계정으로 재현·재검증했다(가짜/테스트 계정만 사용, 실제 학생 계정 조작 없음). CSS 파일은 건드리지 않았다.

### 확인했지만 고치지 않은 것 (이번 CSS 보정이 만든 문제 아님)
조수 이름(`.aname`)에 걸린 `text-decoration:underline dotted`가 원래(수정 전 원본)부터 있던 인라인 스타일인데, 이름이 44px 초상·15px 글자와 함께 보이니 이번 폰트(PureunSup)에서 글자마다 점이 찍힌 것처럼 보인다. 폰트나 이름 표시 방식과 관련된 별개 이슈라 이번 CSS 보정 검증 범위에서는 손대지 않았다. 원하면 후속으로 `text-underline-offset`을 늘리거나 밑줄 대신 다른 강조 방식으로 바꾸는 걸 제안한다.

### 배포
`shinjang_science.html`(CSS 보정), `supabase_schema.sql`(위 SQL 수정) 모두 git에 커밋·푸시했고, SQL은 Supabase에도 직접 반영했다. 재화·성적·SQL 로직 자체는 변경하지 않았다(버그 수정 한 줄 제외).
